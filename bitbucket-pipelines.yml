image: node:10.15.3

pipelines:
  branches:
    master:
      - step:
          name: "Firebase production deployment"
          deployment: production
          caches:
            - node
          script:
            - npm install --quiet
            - npm run build --prod