export interface SaveProfileValues {
    name: string,
    data: string
}

export interface getProfiles {
    createdAt:string,
    data: string,
    name: string,
    img:any
    updatedAt: string,
    _id: string
}