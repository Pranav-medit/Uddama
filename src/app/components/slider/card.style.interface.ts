export interface CardStyleInterface {
    width: number;
    height: number;
    borderRadius: number;
    spaceBetweenCards: number;
    speed: number;
    slidesToShow: number;
    slidesToScroll: number;
}
